// Config file that contains all application level values
// It has not been placed in the config folder, because that upsets sequelize
module.exports = {
  secret: "supersecret",
  tokenExpiration: 300,
  apiVersion: 1,
};
